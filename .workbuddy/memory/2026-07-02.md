# 2026-07-02

## 修复 ShrikeDxf.vcxproj.filters 缺少 Manager 过滤器
- 现象：VS 解决方案资源管理器里看不到 `ShrikeDxf/Manager` 目录（点不进去），但磁盘上 `D:\shrikedxf\ShrikeDxf\Manager` 实际存在 31 个 .h/.cpp。
- 根因：`ShrikeDxf.vcxproj.filters` 没有为 `Manager` 注册任何 `<Filter>` 节点，所有 Manager 文件都被挂到了 `Header Files\Dxf / Header Files\Common / Source Files\Dxf / Source Files\Common` 几个不含 "Manager" 字样的过滤器下，于是 VS 没有显示 `Manager` 虚拟目录。
- 处理：
  1. 在 `<ItemGroup>` 过滤器声明区追加三个节点：`Header Files\Manager`、`Source Files\Manager`、`Form Files\Manager`（GUID 随机生成）。
  2. 批量将 31 个 `Manager\*` 文件的 `<Filter>` 改为：`.h/.h(QtMoc)` → `Header Files\Manager`；`.cpp` → `Source Files\Manager`。
- 验证：31 个条目全部替换；用 `xml.etree.ElementTree` 解析文件，无语法错误。
- 涉及文件：`D:\shrikedxf\ShrikeDxf\ShrikeDxf.vcxproj.filters`

## 解答 "@查看文件的最大限度" + 全项目文件盘点
- 答复：单次 `Read` ≤ 2000 行，文件数无上限（用 `Glob`/`Grep` 配合），真实瓶颈是整轮对话 token 总量。
- 执行：跑了 `.workbuddy/memory/check_projects.py` 脚本，交叉对比 **ShrikeDxf / libdxfrw / dxflib** 三个项目的 `.vcxproj` 声明 vs 磁盘文件。
  - 三个项目**所有声明文件都在磁盘上**（0 MISS）。
  - 磁盘上未声明的文件全是合理的非源：项目元文件（.vcxproj/.filters/.user）、资源 PNG（通过 .qrc）、跨平台构建脚本（CMake/Makefile）、`dwg2dxf/`、`dxflib/`（未加入当前 sln）。
- **结论：无需进一步修改 vcxproj/.filters。所有源文件 @ 引用、编译、解决方案资源管理器都齐全。**

## 拉伸交互链 Review（CDxfEditController / CDxfInteractionDispatcher / CDxfData / CDxfManager）
- 范围：`Manager/{DxfManager.h,DxfEditController.h/cpp,DxfInteractionDispatcher.h/cpp,DxfData.h/cpp}`。
- 主要问题（按风险排）：
  - P0：`DxfInteractionDispatcher::OnMouseMove` 里 `m_pEditCtrl->UpdateStretch(scenePos);` **被注释掉** — 拉伸目前无法实时跟随光标。
  - P0：`DxfEditController::EndStretch` 在 release 时又调一次 `stretch(finalPos)`，与 UpdateStretch 重复。
  - P0：`DxfData.cpp` 11 个 `AddXxx` + `AddEntityToBlock` 全部空 `{}`，调用即静默失败。
  - P1：`GetLayers() const` 内部用 `const_cast` 改写，破坏只读语义。
  - P1：Controller 持有 Manager 指针，与 Manager 持有 Controller unique_ptr 形成循环依赖。
  - P1：选中状态 3 份（Manager / EditController / SelectionController）不同步。
  - P1：夹点容差 `kGripHitTol=12.0` 用的是 scene 坐标，应改成屏幕像素。
  - P1：bbox 四角夹点对 Circle/Arc/Spline 不适用，应按图元类型分夹点策略。
  - P2：缺 Undo/Redo、缺 `DxfData::DxfGraphicsScene.h` 不该 include、命名/分段风格不统一。
- 已生成 P0~P3 修复清单，未动手等用户确认方向。

## 2026-07-03 任务清单 GFM 化（ShrikeDxf.md）
- 将 `D:\shrikedxf\ShrikeDxf.md` 改为 GitHub Flavored Markdown 任务列表。
- 规则：`#` 标题 → `#` 一级；`##` 改二级；`###` 嵌套列表子项；`√` → `[x]`，无勾 → `[ ]`；中英文之间补空格；去行尾空格；`#help` 空标题保留。
- 用户先要预览（已展示），确认后再写入；已写入覆盖原文件。
